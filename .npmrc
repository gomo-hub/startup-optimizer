# === auto-generated by gomo-hub cleanup ===
# Shallow clone: busca apenas o último commit dos repos git+ssh
# Evita download do histórico completo ao instalar @gomo-hub/* via git+ssh
git-shallow-hosts=github-gomo,github.com

# Não executar scripts pós-install (postinstall, prepare, build)
# Segurança + velocidade: evita compilações desnecessárias
ignore-scripts=true

# Ignorar conflitos de peerDependencies entre módulos
legacy-peer-deps=true

# Não travar se lockfile estiver desatualizado (deps git mudam com push)
prefer-frozen-lockfile=false
